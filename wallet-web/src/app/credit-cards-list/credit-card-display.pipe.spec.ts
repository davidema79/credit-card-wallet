import { CreditCardDisplayPipe } from './credit-card-display.pipe';

describe('CreditCardDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new CreditCardDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
